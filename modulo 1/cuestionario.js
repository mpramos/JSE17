// # Respuestas Correctas - Test del Módulo 1

// ## Pregunta 1
// **¿Dónde definitivamente no podremos ejecutar código JavaScript?**
// -a ❌ Directamente en el procesador. 
// -b ❌ En un navegador web.
// -c ❌ En un dispositivo móvil.
// -d ❌ En un entorno de servidor utilizando Node.js.

// ## Pregunta 2
// **El lenguaje TypeScript es:**
// -a ❌ El nombre original de JavaScript, que ha cambiado con el tiempo.
// -b ❌ Una variante de JavaScript que es compilada en lugar de interpretada.
// -c ❌ Un nombre alternativo para JavaScript.
// -d ❌ Un nuevo lenguaje basado en JavaScript que, entre otras cosas, introduce tipado estático.

// ## Pregunta 3
// **El conjunto de herramientas básico necesario para desarrollar código JavaScript de manera efectiva 
// **consta de dos elementos:**
// -a ❌ Editor de código, intérprete.
// -b ❌ Intérprete, depurador.
// -c ❌ Editor de código, depurador.
// -d ❌ Intérprete, gestor de paquetes.

// ## Pregunta 4
// **¿Qué no es tarea del intérprete?**
// -a ❌ Transformar todo el código del programa en código de destino antes de la ejecución.
// -b ❌ Crear un entorno de ejecución para el programa.
// -c ❌ Verificar la corrección del código fuente del programa (sintaxis).
// -d ❌ Transformar los comandos individuales del código fuente en su forma de destino.

// ## Pregunta 5
// **Para mostrar la palabra "test" en la consola usando JavaScript, usamos:**
// -a ❌ `console.log("test");`
// -b ❌ `console (log, "test");`
// -c ❌ `console("test");`
// -d ❌ `log("test");`

// ## Pregunta 6
// **Un programa JavaScript del lado del cliente:**
// -a ❌ Se ejecuta directamente en un navegador web y puede contener un documento HTML dentro de él.
// -b ❌ Requiere soporte del servidor para ejecutarse.
// -c ❌ Puede estar incrustado dentro de un documento HTML o ejecutarse de forma independiente en el navegador.
// -d ❌ Debe estar incrustado dentro de un documento HTML.

// ## Pregunta 7
// **HTML es:**
// -a ❌ Uno de los formatos de compresión de datos.
// -b ❌ Un protocolo de red.
// -c ❌ Un lenguaje para describir la estructura de una página web.
// -d ❌ Un lenguaje de scripting para cálculos matemáticos.

// ## Pregunta 8
// **La etiqueta `<html>` indica el inicio del documento HTML. ¿Qué etiqueta debe aparecer al final del documento?**
// -a ❌ `<lmth>`
// -b ❌ `<html/>`
// -c ❌ `<html>`
// -d ❌ `</html>`

// ## Pregunta 9
// **¿Qué etiqueta utilizamos en HTML para denotar la parte principal del documento donde definimos
// ** los elementos de la página?**
// - ❌ `<content>`
// - ❌ `<main>`
// -c ❌ `<body>`
// - ❌ `<head>`

// ## Pregunta 10
// **¿Qué etiqueta HTML se usa para indicar que estamos incrustando código JavaScript?**
// -a ❌ `<source>`
// -b ❌ `<script>`
// -c ❌ `<js>`
// -d ❌ `<execute>`

// ## Pregunta 11
// **En un navegador, para abrir un archivo local escribiendo su ruta en la barra de direcciones,
// ** la ruta debe ir precedida de:**
// -a ❌ `local:///`
// -b ❌ `http://`
// -c ❌ `file:///`
// -d ❌ `https://`

// ## Pregunta 12
// **En el navegador, al escribir en la barra de direcciones una cadena que comience con `file:///`,
// ** significa que:**
// -a ❌ No queremos abrir la página, sino solo descargar un archivo de la dirección remota y guardarlo en la máquina local.
// -b ❌ El resto de la cadena es la ruta al archivo en nuestro ordenador local que queremos abrir en el navegador.
// -c ❌ Queremos que el navegador cree una página en blanco con el nombre que damos como continuación de la cadena.
// -d ❌ Queremos guardar la página como un archivo local.

// ## Pregunta 13
// **Al ingresar `about:blank` en la barra de direcciones del navegador:**
// -a ❌ Genera una página con información sobre el estado del navegador y la envía al desarrollador.
// -b ❌ Restablece el navegador a su configuración predeterminada.
// -c ❌ Genera y carga una página HTML mínima en blanco en la pestaña actual.
// -d ❌ Abre una pestaña con información sobre tu navegador.

// ## Pregunta 14
// **El código `console.log("http://test.org");` en JavaScript:**
// -a ❌ Mostrará el siguiente mensaje en la consola: `"http://test.org"`.
// -b ❌ Mostrará en la consola información sobre el progreso de carga de la página `http://test.org`.
// -c ❌ Enviará un log con información sobre el script actualmente ejecutado a la dirección `http://test.org`.
// -d ❌ Hará que la página `http://test.org` se cargue en el navegador.
